# Which image `docker-compose.yml` pulls (ghcr.io/lucienkerl/loxmatter:<tag>).
# Without this line Compose's own default (`:-stable`) would apply invisibly -
# here the same value is written out explicitly for two reasons: the
# updater of the next stage rolls back a failed update by
# rewriting exactly this line - an existing line it replaces
# in place, a missing one it would have to append first, and its
# check "forward only, never backward" compares against what is here.
# And "stable" is a moving alias, not a fixed state -
# writing it out makes it visible that this installation follows a channel
# rather than a pinned version, and makes "dev" (the rolling channel
# from the latest main) discoverable as an alternative.
LOXMATTER_IMAGE_TAG=stable

# Operating mode. Empty = Wi-Fi and Ethernet Matter devices only; "thread" also
# adds the OpenThread Border Router (service `otbr`).
#
# Compose reads this variable from the .env by itself - that's why the
# operating mode lives here rather than as a `--profile` argument on every
# call. A later `docker compose up`, `scripts/update.sh` and the watchdog
# all pick the right one automatically as a result.
#
# Retrofitting: plug in the radio module, set `thread` here, point
# RADIO_DEVICE below at the right path, run `docker compose up -d` again.
COMPOSE_PROFILES=thread

# The next three variables (RADIO_DEVICE, RADIO_BAUDRATE, BACKBONE_IF) are
# only needed by the otbr service and therefore only by
# COMPOSE_PROFILES=thread - without that profile otbr is never created and
# none of them is ever read.
RADIO_DEVICE=/dev/ttyUSB0
RADIO_BAUDRATE=460800
BACKBONE_IF=wlan0
# Appended to otbr's radio URL; empty unless set. `&uart-exclusive` asks
# OpenThread to lock its stick against other processes. Not verified against
# the otbr image yet, and an image that refused it would keep Thread down
# after the next otbr recreate - leave it unset unless you are testing it
# (docs/superpowers/specs/2026-09-09-first-run-checklist.md, step 15.4).
# OTBR_RADIO_URL_EXTRA=&uart-exclusive
# The otbr image; empty means loxmatter's own pinned build
# (ghcr.io/lucienkerl/loxmatter-otbr, see deploy/otbr/source.env), not the
# official openthread/otbr:latest, which lacks OpenThread's RCP restoration:
# when the radio stick stops answering, that build's agent tries to restore
# it, up to twice, before it gives up and exits. Nobody has to build that
# image by hand; a newer commit reaches every installation through a
# release (docs/DEVELOPMENT.md, "Moving to a newer OpenThread border
# router"). Set this only to override it, e.g. to point at a locally built
# image while testing a different ot-br-posix commit. The updater only ever
# pulls the loxmatter image, so a local tag is left alone - it just has to
# exist on this host before otbr is next recreated.
OTBR_IMAGE=
# id of the Bluetooth adapter (from `hci0` -> 0) for BLE commissioning by
# matter-server. The Pi only has hci0, hence 0; see the README's "Enable
# BLE".
BLUETOOTH_ADAPTER=0

# IP of the Loxone Miniserver, as seen from this host (for the loxmatter
# service, Phase 4). No sensible default - without this variable
# `loxmatter run` starts with an empty --miniserver address.
MINISERVER_IP=

# Protects the WebUI's `/api` routes (commissioning, removal, fabric
# backup) with `Authorization: Bearer <token>` (Task 8, Phase 5, Spec 9).
# `/cmd`/`/resync` for the Miniserver are unaffected by this.
#
#   OPTIONAL since the WebUI login (Task 9/10, Phase 6). Access to the
#   interface itself no longer runs through this token but through a
#   password set on the first call to `http://<host>:8080/` - until then
#   NO `/api` route returns anything (401), with or without a token, an
#   open state no longer exists. This token remains worthwhile anyway: it's
#   the way for scripts and `curl`, which cannot log in via a browser, and
#   it works independently of the first-run setup status. Anyone who only
#   uses the WebUI can leave this variable empty.
#
# Generate with:
#
#   openssl rand -hex 32
#
# Exactly this way, not any random string: the token must be transmittable
# in an HTTP header and in a WebSocket subprotocol - no spaces, no comma,
# no non-ASCII. `openssl rand -hex 32` produces only [0-9a-f] and satisfies
# that by construction. A token consisting only of whitespace (say, a
# stray line break) counts as "unset".
LOXMATTER_API_TOKEN=
